import { Component, OnInit } from '@angular/core';

@Component({
  selector: 'advanced-table',
  templateUrl: 'advanced-table.component.html'
})

export class AdvancedTableComponent implements OnInit {
  constructor() {
  }

  ngOnInit() {
  }

  /**
   * Implement table component
   *
   * 1. Display data based on given array
   * 2. On select highlights selected row
   * 3. On select emits event selectedRow
   * 4. Allows user to sort data
   *
   */

}
